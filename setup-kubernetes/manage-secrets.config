#!/bin/bash
# manage-secrets.config
# Files to backup and restore with manage-secrets.sh
# Uses absolute paths — glob patterns are supported.

SECRET_FILES=(
    "${HOME}/secrets/kube-*.txt"
    "${HOME}/secrets/argocd-*.txt"
    "${HOME}/secrets/vault-*.txt"
)
