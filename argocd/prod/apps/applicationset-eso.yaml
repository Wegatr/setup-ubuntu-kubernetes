# Per-namespace External-Secrets-Operator controllers for the PROD cluster.
# See argocd/dev/apps/applicationset-eso.yaml for the full rationale.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-eso-prod
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error

  generators:
    - list:
        elements:
          - ns: mongodb
          - ns: postgresql
          - ns: postfix
          - ns: seq
          - ns: dbgate
          - ns: observability

  template:
    metadata:
      name: "external-secrets-{{ .ns }}-prod"
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: "1"
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      sources:
        - repoURL: &repo "https://github.com/kartalbas/setup-ubuntu-kubernetes.git"
          targetRevision: &branch main
          ref: values
        - repoURL: *repo
          targetRevision: *branch
          path: apps/external-secrets
          helm:
            valueFiles:
              - $values/platform/values-common.yaml
              - $values/platform/values-prod.yaml
              - values-common.yaml
              - values-prod.yaml
            valuesObject:
              external-secrets:
                scopedNamespace: "{{ .ns }}"
      destination:
        server: "https://kubernetes.default.svc"
        namespace: "{{ .ns }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
