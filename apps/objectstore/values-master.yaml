# MASTER-env objectstore config — only the Vault paths differ per env.
externalsecret-garage:
  externalSecret:
    vaultPath: master/app/objectstore

externalsecret-filestash:
  externalSecret:
    vaultPath: master/app/objectstore
