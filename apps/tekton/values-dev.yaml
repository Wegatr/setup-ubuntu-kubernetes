# DEV-specific Tekton overrides. Phase A: only DEV runs the image-build
# pipelines + the Dashboard UI. Auth is gated by apps/login (shared
# oauth2-proxy + apex cookie) — no per-app auth bits remain here.
dashboard:
  enabled: true

ingress:
  ingress:
    enabled: true
